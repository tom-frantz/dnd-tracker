import React from 'react';
import {Text} from "react-native";


interface SomethingProps
{
}

const Something: React.FC < SomethingProps > = (props: SomethingProps) => {
    return <Text>THis is nateive</Text>
};

export default Something;
